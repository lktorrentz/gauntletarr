<?xml version="1.0"?>
<Container version="2">
  <Name>gauntletarr</Name>
  <Repository>ghcr.io/lktorrentz/gauntletarr:latest</Repository>
  <Registry>https://ghcr.io/lktorrentz/gauntletarr</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/lktorrentz/gauntletarr/issues</Support>
  <Project>https://github.com/lktorrentz/gauntletarr</Project>
  <Overview>The Media Gauntlet*rr manages your media library, torrent seeding folders, the hardlink correspondence between them, and the real state of your configured torrent clients (multi-client, cross-seed included) in one place.&#13;
&#13;
WARNING - early development build (Phase 2 of docs/ROADMAP.md): only the filesystem/hardlink scan and the qBittorrent adapter are implemented so far. TMDB identification, the library view, the reseeding engine and uploads land in later phases - this container currently only exposes a JSON API (no web UI yet). Useful mainly to verify the qBittorrent adapter against a real instance.&#13;
&#13;
Follows the TrashGuide layout: mount your combined torrents+media folder at /data, same as your download client and media manager - this is what makes hardlinks between them work. Only two paths are needed: Config (appdata) and Data (your library). No need to edit any file: once /data is mounted, register it as a disk via the API (POST /api/disks) - no web UI for this yet.&#13;
&#13;
On first start, if config/config.yaml doesn't exist yet in the "Config" folder below, the container creates it from config.example.yaml with default values.</Overview>
  <Category>Tools: Downloaders:</Category>
  <WebUI>http://[IP]:[PORT:8080]/api/health</WebUI>
  <TemplateURL/>
  <Icon></Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>
  <Config Name="WebUI Port" Target="8080" Default="8080" Mode="tcp" Description="API port (no web UI yet)" Type="Port" Display="always" Required="true" Mask="false">8080</Config>
  <Config Name="Config" Target="/app/config" Default="/mnt/user/appdata/gauntletarr/config" Mode="rw" Description="Appdata: config.yaml (created from config.example.yaml on first start if missing) and the app's own SQLite database/cache" Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/gauntletarr/config</Config>
  <Config Name="Data" Target="/data" Default="/mnt/user/data" Mode="rw" Description="TrashGuide-style combined torrents+media folder - mount the SAME host path you use for your download client and media manager, so hardlinks between them work. Registered as a disk via POST /api/disks after first start." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/data</Config>
  <Config Name="PUID" Target="PUID" Default="99" Mode="" Description="User ID the web process runs as (99 = nobody on Unraid, matching Sonarr/Radarr/qBittorrent)" Type="Variable" Display="always" Required="false" Mask="false">99</Config>
  <Config Name="PGID" Target="PGID" Default="100" Mode="" Description="Group ID the web process runs as (100 = users on Unraid)" Type="Variable" Display="always" Required="false" Mask="false">100</Config>
  <Config Name="TZ" Target="TZ" Default="Europe/Rome" Mode="" Description="Timezone" Type="Variable" Display="always" Required="false" Mask="false">Europe/Rome</Config>
  <Config Name="APP_SECRET_KEY" Target="APP_SECRET_KEY" Default="" Mode="" Description="Key used to encrypt credentials stored in the database at rest (tracker tokens, torrent client passwords). Generate with: python3 -c &quot;from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())&quot;" Type="Variable" Display="always" Required="true" Mask="true"></Config>
</Container>
